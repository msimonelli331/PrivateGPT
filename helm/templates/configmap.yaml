apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "privategpt.fullname" . }}
data:
{{- range $path, $_ :=  .Files.Glob  "configs/*" }}
  {{ $path | replace "configs/" "" }}: |
{{ $.Files.Get $path | indent 4 }}
{{- end }}