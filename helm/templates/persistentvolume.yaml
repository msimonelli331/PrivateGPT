{{- if .Values.persistentVolume.enabled -}}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ template "privategpt.fullname" . }}
  labels: {{- include "privategpt.labels" . | nindent 4 }}
spec:
  capacity:
    storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  local:
    path: {{ .Values.persistentVolume.hostPath }}
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: {{ template "privategpt.fullname" . }}
          operator: In
          values:
          - local
{{- end }}