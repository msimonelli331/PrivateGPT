apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "privategpt.fullname" . }}-test-connection"
  labels:
    {{- include "privategpt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "privategpt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
